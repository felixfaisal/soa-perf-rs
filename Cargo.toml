[package]
name = "soa-perf-rs"
version = "0.1.0"
edition = "2024"

[[bench]]
name = "layout_bench"
harness = false

[dependencies]
rand = "0.8"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
